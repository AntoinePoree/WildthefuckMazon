import { TestBed, async } from '@angular/core/testing';
import { AddProduitsComponent } from './addProduit.component';

/*describe('Component: AddProduits', () => {
  it('should create an instance', () => {
    let component = new AddProduitsComponent();
    expect(component).toBeTruthy();
  });
});*/
